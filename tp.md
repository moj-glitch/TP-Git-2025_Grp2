hello, ceci est le tp.md de ilan rossi
                case QUIT:
                    printf("Quit event received.\n");
                    QuitWindow(&myWindow);
                    break;
                default:
                    break;

